<h1> Welcome </h1>

<% @albums.each do |album| %>
    <p><%= album.title %></p>
    <p><%= album.artist.name %></p>
    <p><%= album.stock %> <%= album.stock_status %></p>
    <p><%= album.cover_url %></p>
<% end %>
